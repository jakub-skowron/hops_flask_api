GET the hops 
---
tags:
  - Hops
parameters:
  - in: query
    name: page
    schema:
      type: integer
    description: The page number
  - in: query
    name: per-page
    schema:
      type: integer
    description: The amount of hops per page
  - in: query
    name: name
    schema:
      type: string
    description: The hop name or part of the name
  - in: query
    name: alpha-max-percentage
    schema:
      type: integer
    description: The percentage max value of alpha acids
  - in: query
    name: alpha-min-percentage
    schema:
      type: integer
    description: The percentage min value of alpha acids
  - in: query
    name: beta-max-percentage
    schema:
      type: integer
    description: The percentage max value of beta acids
  - in: query
    name: beta-min-percentage
    schema:
      type: integer
    description: The percentage min value of beta acids
  - in: query
    name: origin
    schema:
      type: string
    description: The hop's origin country
  - in: query
    name: description
    schema:
      type: string
    description: The hop's description
  - in: query
    name: aroma
    schema:
      type: string
    description: The hop's aroma
  - in: query
    name: beer-styles
    schema:
      type: string
    description: Possibility to search kind of hops by beer style which perfectly suits with it
  - in: query
    name: used-for
    schema:
      type: string
    description: The hop's characteristic
  - in: query
    name: substitutions
    schema:
      type: string
    description: The hop's substitutions
responses:  
  200:
    description: Information about the hops
  400:
    description: Bad request